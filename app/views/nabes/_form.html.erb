<%= form_for(@nabe) do |f| %>
  <% if @nabe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nabe.errors.count, "error") %> prohibited this nabe from being saved:</h2>

      <ul>
      <% @nabe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
